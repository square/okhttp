plugins {
  id "application"
}

tasks.withType(JavaCompile) {
  options.warnings = false
}

application {
  mainClassName = 'okhttp3.sample.SampleServer'
}

dependencies {
  implementation project(':mockwebserver')
}
